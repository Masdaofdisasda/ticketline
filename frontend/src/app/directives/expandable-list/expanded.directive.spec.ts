import { ExpandedDirective } from './expanded.directive';

describe('ExpandedDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandedDirective();
    expect(directive).toBeTruthy();
  });
});
