package at.ac.tuwien.sepm.groupphase.backend.endpoint.mapper;

class VenueMapperTest {
/*
  VenueMapper mapper = VenueMapper.INSTANCE;

  Venue venue = Venue.builder()
    .id(0L)
    .city("Vienna")
    .country("Austria")
    .name("Stadthalle")
    .street("Straße")
    .zipCode("0992")
    .build();

  VenueDto venueDto = VenueDto.builder()
    .id(1L)
    .city("Salzburg")
    .country("Austria")
    .name("Konzerthalle")
    .street("Str")
    .zipCode("9002")
    .build();

  @Test
  void locationDtoToLocation() {
    Venue currentVenue = mapper.venueDtoToVenue(venueDto);

    assertAll(() -> {
      assertThat(currentVenue.getId()).isEqualTo(venueDto.getId());
      assertThat(currentVenue.getCity()).isEqualTo(venueDto.getCity());
      assertThat(currentVenue.getCountry()).isEqualTo(venueDto.getCountry());
      assertThat(currentVenue.getName()).isEqualTo(venueDto.getName());
      assertThat(currentVenue.getStreet()).isEqualTo(venueDto.getStreet());
      assertThat(currentVenue.getZipCode()).isEqualTo(venueDto.getZipCode());
    });
  }

  @Test
  void locationToLocationDto() {
    VenueDto currentDto = mapper.venueToVenueDto(venue);

    assertAll(() -> {
      assertThat(currentDto.getId()).isEqualTo(venue.getId());
      assertThat(currentDto.getCity()).isEqualTo(venue.getCity());
      assertThat(currentDto.getCountry()).isEqualTo(venue.getCountry());
      assertThat(currentDto.getName()).isEqualTo(venue.getName());
      assertThat(currentDto.getStreet()).isEqualTo(venue.getStreet());
      assertThat(currentDto.getZipCode()).isEqualTo(venue.getZipCode());
    });
  }
  *
 */
}